name: Unit tests

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    container: fastar/ml-base:slim

    steps:
    - uses: actions/checkout@v1
    - name: Install pytest
      run: pip install pytest
    - name: Run pytest
      run: pytest
